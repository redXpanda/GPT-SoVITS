--no-binary=opencc
numpy<2.0
scipy
tensorboard
librosa==0.10.2
numba
pytorch-lightning>=2.4
gradio<5
ffmpeg-python
onnxruntime; platform_machine == "aarch64" or platform_machine == "arm64"
onnxruntime-gpu; platform_machine == "x86_64" or platform_machine == "AMD64"
tqdm
funasr>=1.3.7
cn2an
pypinyin
pyopenjtalk>=0.4.1
g2p_en
# torch 版本钉死: 2.11+ 与本仓库训练代码(旧式 DDP/autocast)错配, 反向时 0xC0000005 崩溃
# 注: cu 后缀(cu124)由安装脚本的 --index-url 决定, requirements 只钉版本号
torch==2.6.0
torchaudio==2.6.0
# torchcodec  # 仅 torchaudio>=2.8 需要; 降级到 2.6 后用旧 soundfile 后端, 留着会把 torch 顶回 2.11
modelscope
sentencepiece
transformers>=4.51,<=4.51.3  # 升级以支持 Fun-ASR-Nano-2512 的 qwen3 架构
peft<0.18.0
chardet
PyYAML
psutil
jieba_fast
jieba
split-lang
fast_langdetect>=0.3.1
wordsegment
rotary_embedding_torch
ToJyutping 
g2pk2
ko_pron
opencc
python_mecab_ko; sys_platform != 'win32'
fastapi[standard]>=0.115.2,<0.116
x_transformers
torchmetrics<=1.5
pydantic<=2.10.6
ctranslate2>=4.0,<5
av>=11
tqdm
